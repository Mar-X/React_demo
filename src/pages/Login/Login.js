import React from 'react';
import './Login.css'

const login = (props) => {
    return (
        <div>
            <h1>登录页</h1>
            <button>进入</button>
        </div>
    )
}

export default login